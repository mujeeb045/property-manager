<h1 class="text-3xl font-bold mb-8">👥 Manage Active Tenants</h1>

<% if (error) { %>
    <div class="bg-red-100 text-red-700 p-4 rounded-xl mb-6">
        <%= error %>
    </div>
    <% } %>

        <div class="max-w-5xl mx-auto space-y-6">
            <%- rowsHTML %>
        </div>

        <div class="mt-10 text-center">
            <a href="/register-tenant"
                class="inline-block bg-emerald-600 hover:bg-emerald-700 text-white px-8 py-4 rounded-2xl font-medium text-lg">
                + Register New Tenant
            </a>
        </div>